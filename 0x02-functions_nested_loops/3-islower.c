#include "main.h"

/**
* _islower - checks for lowercase character 
*
* return: 1 if its lowercase and 0 otherwise
*/
int _islower(int c)
{

	if(c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return(0);
	}

	return(0);
}
